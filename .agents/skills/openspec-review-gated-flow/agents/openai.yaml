interface:
  display_name: "OpenSpec Review-Gated Flow"
  short_description: "Gate semantics and auto-land bookkeeping"
  default_prompt: "Use $openspec-review-gated-flow to gate semantic OpenSpec work and auto-land only mechanical reconciliation."
