## 1. Marker Implementation

- [ ] 1.1 Create `canary/terminal-archive-v10.txt` with exactly `terminal-mechanical-archive-v10` followed by one newline.

## 2. Deterministic Verification

- [ ] 2.1 Add one focused Node test that resolves the marker from the repository, verifies it exists, and compares its exact newline-terminated content.
- [ ] 2.2 Run the focused Node test and `openspec validate sac-118 --strict`.
