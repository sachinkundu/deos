## 1. Verify provider contracts

- [x] 1.1 Capture real labeled and unlabeled Linear Todo events and prove exact event-time label evidence.
- [x] 1.2 Create real Basic and Standard-2 test sandboxes and record the provider mapping.

## 2. Freeze delivery and run tier facts

- [x] 2.1 Add additive delivery, run, attempt, dispatch-failure, and trial migrations with guarded legacy backfill.
- [x] 2.2 Save positive slow-ok evidence and the release policy on authenticated deliveries and Queue messages.
- [x] 2.3 Validate durable delivery agreement, terminalize integrity failures, and freeze the tier in guarded run allocation.
- [x] 2.4 Prove duplicate handling, replay, transient storage errors, and immutable run selection.

## 3. Execute with the saved tier

- [x] 3.1 Reserve every sandbox attempt with its run tier and reject invalid or mismatched values.
- [x] 3.2 Route sandbox creation, retry, reconciliation, and cleanup through the verified tier mapping.
- [x] 3.3 Record trusted attempt timing and tier telemetry; preserve provider failures without fallback.

## 4. Expose run and trial evidence

- [x] 4.1 Show the saved run tier and route start failures in the DEOS workflow portal.
- [x] 4.2 Add the protected immutable trial manifest, guarded run binding, and paired report with visible exclusions.
- [x] 4.3 Validate portal behavior and trial counts, failed attempts, retries, and mismatched pairs.

## 5. Migrate and activate

- [ ] 5.1 Validate compatibility writers, null-only legacy backfill, and zero invalid or mismatched tiers.
- [ ] 5.2 Prove account capacity and configure creation-failure alerts with the rollback response procedure.
- [ ] 5.3 Drain pre-activation starts, activate event-label-v1 at 100 percent traffic, and read back deployment state.
- [ ] 5.4 Capture provider-originated runs for both choices, portal screenshots, and a clean 20-attempt Standard-2 window.
- [ ] 5.5 Run repository checks and publish tasks, implementation, and evidence in one review-ready PR.
